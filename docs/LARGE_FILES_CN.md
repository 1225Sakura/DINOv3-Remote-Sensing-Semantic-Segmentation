# 大文件下载说明（中文版）

本项目包含一些大文件（模型权重、日志文件等），这些文件已被`.gitignore`排除，不会上传到GitHub。您可以从百度网盘下载：

## 📦 百度网盘下载

**分享名称**: dinov3-遥感语义

**下载链接**: https://pan.baidu.com/s/5CXLX9bODEHBSVfKVRLsmdg

**网盘目录结构**:
```
dinov3-遥感语义/
├── model/           # 训练好的模型文件（约7.2GB）
│   └── trained_models/
│       ├── isaid/
│       ├── landcoverai/
│       ├── loveda/
│       ├── openearthmap/
│       ├── potsdam/
│       └── vaihingen/
└── 数据集/          # 6个遥感数据集（需要的话可以下载）
    ├── LoveDA/
    ├── iSAID/
    ├── Vaihingen/
    ├── Potsdam/
    ├── LandCoverai/
    └── OpenEarthMap/
```

> **提示**:
> - 如果只需要使用模型进行预测，下载 `model` 文件夹即可
> - 如果需要训练或完整数据集，可以下载 `数据集` 文件夹

---

## 📋 文件清单

### 1️⃣ 训练好的模型文件（总计约 7.2 GB）

所有模型文件位于 `trained_models/` 目录下，每个数据集一个子目录：

```
trained_models/
├── isaid/
│   ├── model.pth          (1.2 GB) - iSAID数据集训练的模型
│   └── results.json       (4 KB)   - 训练结果和指标
├── landcoverai/
│   ├── model.pth          (1.2 GB) - LandCover.ai数据集训练的模型
│   └── results.json       (1 KB)
├── loveda/
│   ├── model.pth          (1.2 GB) - LoveDA数据集训练的模型
│   └── results.json       (4 KB)
├── openearthmap/
│   ├── model.pth          (1.2 GB) - OpenEarthMap数据集训练的模型
│   └── results.json       (4 KB)
├── potsdam/
│   ├── model.pth          (1.2 GB) - Potsdam数据集训练的模型
│   └── results.json       (4 KB)
└── vaihingen/
    ├── model.pth          (1.2 GB) - Vaihingen数据集训练的模型
    └── results.json       (1 KB)
```

### 2️⃣ 训练日志文件（总计约 229 MB）

日志文件位于 `logs/` 目录：

```
logs/
├── training_output.log           (97 MB)  - 主训练输出日志
├── full_training_fixed.log       (54 MB)  - 完整训练日志（修正版）
├── full_training.log             (47 MB)  - 完整训练日志
└── training_output_previous.log  (31 MB)  - 之前的训练输出日志
```

**注意**：日志文件仅供参考训练过程，不是必需的。如果您只想使用模型进行预测，可以不下载日志文件。

### 3️⃣ DINOv3 预训练权重（可选，约 1-2 GB）

如需从头训练模型，建议下载DINOv3预训练权重：

- **官方来源**: [DINOv3 GitHub](https://github.com/facebookresearch/dinov3)
- **推荐模型**: `dinov3_vitl16_pretrain_lvd1689m.pth`
- **放置位置**: 创建 `checkpoints/` 文件夹并放入

### 4️⃣ 数据集（需单独下载）

训练使用的6个遥感数据集需要从官方网站下载：

| 数据集 | 类别数 | 官方下载链接 | 放置路径 |
|--------|--------|-------------|----------|
| LoveDA | 7 | [GitHub](https://github.com/Junjue-Wang/LoveDA) | `datasets/LoveDA/` |
| iSAID | 16 | [官网](https://captain-whu.github.io/iSAID/) | `datasets/iSAID/` |
| Vaihingen | 5 | [ISPRS](https://www.isprs.org/education/benchmarks/UrbanSemLab/) | `datasets/Vaihingen/` |
| Potsdam | 6 | [ISPRS](https://www.isprs.org/education/benchmarks/UrbanSemLab/) | `datasets/Potsdam/` |
| LandCover.ai | 5 | [官网](https://landcover.ai/) | `datasets/LandCoverai/` |
| OpenEarthMap | 8 | [官网](https://open-earth-map.org/) | `datasets/OpenEarthMap/` |

**详细配置说明**：请参考 [数据集设置指南](DATASET_SETUP_CN.md)

---

## 🚀 使用说明

### 下载后的文件放置

从百度网盘下载的压缩包，解压后按以下结构放置：

```
remote_sensing_segmentation_project/
├── trained_models/          # 训练好的模型（从网盘下载）
│   ├── isaid/
│   ├── landcoverai/
│   ├── loveda/
│   ├── openearthmap/
│   ├── potsdam/
│   └── vaihingen/
├── logs/                    # 训练日志（可选，从网盘下载）
│   ├── training_output.log
│   └── ...
├── checkpoints/             # 预训练权重（可选，从官方下载）
│   └── dinov3_vitl16_pretrain.pth
└── datasets/                # 数据集（从官方网站下载）
    ├── LoveDA/
    ├── iSAID/
    ├── Vaihingen/
    ├── Potsdam/
    ├── LandCoverai/
    └── OpenEarthMap/
```

### 验证下载完整性

#### Linux/Mac:
```bash
# 检查模型文件是否存在
ls -lh trained_models/*/model.pth

# 查看训练结果
cat trained_models/loveda/results.json | python -m json.tool
```

#### Windows PowerShell:
```powershell
# 检查模型文件
Get-ChildItem trained_models\*\model.pth

# 查看训练结果
Get-Content trained_models\loveda\results.json | ConvertFrom-Json
```

---

## 💡 使用场景

### 场景1：直接使用训练好的模型进行预测

**需要下载**：
- ✅ 对应数据集的模型文件（例如 `trained_models/loveda/`）
- ✅ 对应的测试数据集

**不需要下载**：
- ❌ 日志文件
- ❌ 预训练权重
- ❌ 其他数据集的模型

**使用命令**：
```bash
# 对LoveDA数据集进行预测
python scripts/generate_predictions.py \
    --datasets loveda \
    --models_dir trained_models \
    --output_dir predictions
```

### 场景2：从头开始训练模型

**需要下载**：
- ✅ 预训练权重（`checkpoints/`）
- ✅ 训练数据集

**不需要下载**：
- ❌ 已训练的模型文件
- ❌ 日志文件

**使用命令**：
```bash
# 训练LoveDA数据集
python scripts/train.py \
    --datasets loveda \
    --pretrained_weights checkpoints/dinov3_vitl16_pretrain.pth \
    --epochs 100
```

### 场景3：研究训练过程和调试

**需要下载**：
- ✅ 模型文件
- ✅ 日志文件
- ✅ 数据集

---

## 📊 训练结果概览

以下是各数据集的训练结果（详见各目录下的 `results.json`）：

| 数据集 | mIoU | 准确率 | 训练轮数 |
|--------|------|--------|----------|
| LoveDA | ~XX% | ~XX% | XX epochs |
| iSAID | ~XX% | ~XX% | XX epochs |
| Vaihingen | ~XX% | ~XX% | XX epochs |
| Potsdam | ~XX% | ~XX% | XX epochs |
| LandCover.ai | ~XX% | ~XX% | XX epochs |
| OpenEarthMap | ~XX% | ~XX% | XX epochs |

*注：具体指标请查看 `trained_models/<dataset>/results.json` 文件*

---

## ❓ 常见问题

### Q1: 我只想测试某个数据集，需要下载所有模型吗？
**A**: 不需要！只下载对应数据集的模型文件即可。例如只测试LoveDA：
- 下载：`trained_models/loveda/` 目录
- 不需要：其他5个数据集的模型

### Q2: 百度网盘下载速度慢怎么办？
**A**:
1. 可以使用百度网盘客户端，速度会快一些
2. 如果只需要某个数据集的模型，可以在issue中留言，我可以单独分享
3. 考虑升级百度网盘会员（下载速度更快）

### Q3: 日志文件必须下载吗？
**A**: 不必须。日志文件仅记录训练过程，包括：
- 每个epoch的训练损失
- 验证集指标变化
- 模型保存记录

如果只是使用模型进行预测，完全可以不下载日志文件。

### Q4: 如何使用下载的模型进行预测？
**A**:
```bash
# 基本用法
python scripts/generate_predictions.py \
    --datasets <dataset_name> \
    --models_dir trained_models

# 示例：对LoveDA生成预测
python scripts/generate_predictions.py \
    --datasets loveda \
    --models_dir trained_models \
    --output_dir predictions
```

更多使用说明参考主 [README_CN.md](../README_CN.md)

### Q5: 模型文件很大，有压缩过吗？
**A**: 模型文件（.pth）本身已经是紧凑的二进制格式，进一步压缩效果有限。每个模型约1.2GB是DINOv3-Large架构的正常大小。

### Q6: 可以在CPU上使用这些模型吗？
**A**: 可以，但速度会很慢。建议配置：
- **推荐**: GPU (至少8GB显存)
- **可用**: CPU (预测会很慢，不推荐训练)

---

## 🔧 技术支持

如遇问题，请按以下方式寻求帮助：

1. **下载链接失效**: 在项目中提交Issue
2. **文件损坏**: 检查文件MD5，重新下载
3. **使用问题**: 参考 [README_CN.md](../README_CN.md) 或提交Issue
4. **数据集问题**: 参考 [DATASET_SETUP_CN.md](DATASET_SETUP_CN.md)

---

## 📝 更新日志

- **2024-01-XX**: 初始版本，包含6个数据集的训练模型
- 如有模型更新，会在此记录

---

## 📄 许可说明

- **模型权重**: 仅供学术研究和非商业用途
- **数据集**: 遵循各数据集官方的许可协议
- **代码**: 遵循项目的开源许可
